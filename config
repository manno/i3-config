# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

client.focused          #bcbcbc #0087af #ffffff #ff00ff
client.focused_inactive #bcbcbc #a8a8a8 #2a2a2a #ff5f5f
client.unfocused        #bcbcbc #d0d0d0 #3a3a3a #ff5f5f
client.urgent           #ff00ff #ff00ff #ffffff #bcbcbc

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+t exec i3-sensible-terminal
#bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+s floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# move workspace to other monitor
bindsym $mod+z move workspace to output left
bindsym $mod+x move workspace to output right

# switch to workspace
bindsym $mod+1 workspace 1: Term
bindsym $mod+2 workspace 2: Firefox
bindsym $mod+3 workspace 3: Chrome
bindsym $mod+4 workspace 4: Konsole
bindsym $mod+5 workspace 5: ⚛
bindsym $mod+6 workspace 6: COM☎
bindsym $mod+7 workspace 7: ✭
bindsym $mod+8 workspace 8: vnc
bindsym $mod+9 workspace 9: Pass
bindsym $mod+0 workspace 10: Video
bindsym $mod+F1 workspace F1: Shell
bindsym $mod+F2 workspace F2: Mail✉
bindsym $mod+F3 workspace F3: Music♫
bindsym $mod+F4 workspace F4: ☕
bindsym $mod+F5 workspace F5: Merge

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1: Term
bindsym $mod+Shift+2 move container to workspace 2: Firefox
bindsym $mod+Shift+3 move container to workspace 3: Chrome
bindsym $mod+Shift+4 move container to workspace 4: Konsole
bindsym $mod+Shift+5 move container to workspace 5: ⚛
bindsym $mod+Shift+6 move container to workspace 6: COM☎
bindsym $mod+Shift+7 move container to workspace 7: ✭
bindsym $mod+Shift+8 move container to workspace 8: vnc
bindsym $mod+Shift+9 move container to workspace 9: Pass
bindsym $mod+Shift+0 move container to workspace 10: Video
bindsym $mod+Shift+F1 move container to workspace F1: Shell
bindsym $mod+Shift+F2 move container to workspace F2: Mail✉
bindsym $mod+Shift+F3 move container to workspace F3: Music♫
bindsym $mod+Shift+F4 move container to workspace F4: ☕
bindsym $mod+Shift+F5 move container to workspace F5: Merge

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status -c ~/.config/i3/i3status.conf | ~/.config/i3/bin/i3status.py
        tray_output primary
        colors {
            background #232323
            statusline #DCDCDC
        }
}

# Disable mouse warping
mouse_warping none

# press same key again to return to previous workspace
workspace_auto_back_and_forth yes

# bind alt-grave (prev) and alt-tab (overview?) to something?
# https://wiki.archlinux.org/index.php/i3#Quickly_jumping_to_open_window
bindsym $alt+grave workspace prev
bindsym $mod+grave workspace prev
bindsym $alt+Tab workspace next
bindsym $mod+Tab workspace next

# ------------------------------------------------------------------------------
#
# KEYS EXEC APP
#

# shutdown/suspend button?
bindsym $mod+Delete exec ~/.config/i3/bin/i3exit.py

# reset xrandr displays
bindsym $mod+p exec ~/.config/i3/bin/xrandr.py

# screenshot key
bindsym Print exec gnome-screenshot

# control Volume with Volume Keys
bindsym XF86AudioMicMute     exec "pactl set-sink-mute 0 -- toggle; notify-send 'Volume' `~/.config/i3/bin/volume-info.sh`"

# screen lock
bindsym $alt+Control+l exec gnome-screensaver-command -l

# ------------------------------------------------------------------------------
#
# RULES
#

# app specific settings
#assign [class="Evolution"] → 6: COM☎
#assign [class="MPlayer"] → 10: Video
#assign [class="Tomboy"] → 6: COM☎
assign [class="Firefox"] → 2: Firefox
assign [class="Gitk"] → F5: Merge
assign [class="Google-chrome-stable"] → 3: Chrome
assign [class="Keepassx"] → 9: Pass
assign [class="Meld"] → F5: Merge
assign [class="Pidgin"] → 6: COM☎
assign [class="Screenkey"] → 6:
assign [class="X2GoAgent"] → 8: vnc
assign [class="mpv"] → 10: video
assign [class="sun-awt-X11-XFramePeer"] → 6:
assign [title="Java iKVM Viewer"] → 6:

#for_window [class="Gitk"] fullscreen
for_window [class="Eog"] floating enable
for_window [class="Evince"] floating enable
for_window [class="Gitg"] fullscreen
for_window [class="Gmrun" instance="gmrun"] floating enable
for_window [class="MPlayer"] floating enable
for_window [class="Meld"] fullscreen
for_window [class="Nm-applet"] floating enable
for_window [class="Screenkey"] floating enable
for_window [class="Signon-ui"] floating enable
for_window [class="Signon-ui"] floating enable
for_window [class="TeamViewer.exe"] floating enable
for_window [class="Tomboy"] floating enable
for_window [class="Wine"] floating enable
for_window [class="X2goclient" instance="x2goclient"] floating enable
for_window [class="sun-awt-X11-XFramePeer"] floating disable
for_window [title="Steam - Update News"] floating enable
for_window [title="TeamViewer Panel (minimized)"] floating enable, move absolute position 900 px 0 px
for_window [title="TeamViewer Panel"] floating enable, move absolute position 900 px 0 px
for_window [title="TeamViewer"] floating enable, move absolute position 0 px 0 px

# ------------------------------------------------------------------------------
#
# STARTUP
#

# volume indicator / mousewheel
exec --no-startup-id ~/.config/i3/bin/pa-applet

# notes
exec --no-startup-id tomboy

# gnome
exec --no-startup-id /usr/bin/caffeine-plus

# clipboard
exec --no-startup-id /usr/bin/autocutsel
exec --no-startup-id /usr/bin/autocutsel -s PRIMARY

# xmpp
#exec --no-startup-id ~/.config/i3/bin/pidgin

# start programs
exec --no-startup-id /opt/synapse/bin/synapse -s

# network manager
exec --no-startup-id /usr/bin/nm-applet

# compositing
exec_always --no-startup-id ~/.config/i3/bin/compton start

# Touchpad disable, in absence of g-s
exec_always --no-startup-id ~/.config/i3/bin/xinput-tp.sh

# Compose key, needs to be applied to every new keyboard
exec_always --no-startup-id ~/.config/i3/bin/keymap

# gnome
exec_always --no-startup-id /usr/lib/x86_64-linux-gnu/notify-osd

# ------------------------------------------------------------------------------
#
# END
